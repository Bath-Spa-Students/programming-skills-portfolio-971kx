def display_message(topic="functions"):
    print(f"In this chapter, I am learning about functions in Python.")

# Call the function to display the message with the default topic
display_message()

# Call the function with a different topic
display_message("loops")
